# Frontier Lang HTML Adapter Notes

Keep this package focused on runtime-neutral HTML projection and semantic merge evidence. Runtime-neutral shared graph primitives belong in `@shapeshift-labs/frontier-lang-kernel`; compiler facade integration belongs in `@shapeshift-labs/frontier-lang-compiler`.

Do not print or commit npm tokens. Release credentials live in `/Users/james/Documents/json-diff/.env`.
